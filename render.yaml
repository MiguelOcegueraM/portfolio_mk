services:
  - type: web
    name: mikeoceguera-portfolio
    runtime: static
    buildCommand: NODE_OPTIONS=--openssl-legacy-provider npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
